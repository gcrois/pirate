## Economy — shared market definitions only.
## Each Actor owns its own inventory and gold.
extends Node

const GOODS: Array       = ["Rum", "Spices", "Timber"]
const BASE_PRICES: Dictionary = {"Rum": 50, "Spices": 90, "Timber": 35}
